# Pose refinement remains intentionally disabled until diagnostics prove pose error.
OPTIMIZATION:
  geometry_aware: true
  densification_method: hybrid
  densify_until_iter: 15000
  densification_interval: 100
  exposure_compensation: true
  loss_schedule_enabled: true
PIPELINE:
  antialiasing: true
