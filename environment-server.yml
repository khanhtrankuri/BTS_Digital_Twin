name: BTS
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  # Linux/server environment for Gaussian Splatting.
  # The server should already have a recent NVIDIA driver plus gcc/g++ installed.
  - python=3.12
  - pip
  - setuptools
  - wheel
  - tqdm
  - plyfile
  - cmake
  - ninja
  - pytest
  - cuda-nvcc=12.8
  - pip:
      - --extra-index-url https://download.pytorch.org/whl/cu128
      - torch==2.7.1+cu128
      - torchvision==0.22.1+cu128
      - torchaudio==2.7.1+cu128
      - opencv-python
      - joblib
      - pyyaml

# After creating the env from this file, build the local CUDA extensions from
# the repository root:
#
#   conda env create -f environment-server.yml
#   conda activate BTS
#   python -m pip install --no-build-isolation submodules/diff-gaussian-rasterization submodules/simple-knn submodules/fused-ssim
#
# The --no-build-isolation flag is required because these setup.py files import
# torch while building. Conda's environment.yml pip section cannot pass that flag.
