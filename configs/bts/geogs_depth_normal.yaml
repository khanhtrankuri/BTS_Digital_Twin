MODEL:
  GEOMETRY_AWARE:
    ENABLED: true
    DEPTH_LOSS_ENABLED: true
    DEPTH_LOSS_WEIGHT: 0.05
    DEPTH_CONFIDENCE_WEIGHTED: true
    DEPTH_MIN_CONFIDENCE: 0.3
    NORMAL_LOSS_ENABLED: true
    NORMAL_LOSS_WEIGHT: 0.02
    NORMAL_USE_ABS_COSINE: true
    SCALE_REG_ENABLED: true
    SCALE_REG_WEIGHT: 0.001
    MAX_GAUSSIAN_SCALE: 0.1
    MAX_ANISOTROPY_RATIO: 20.0
DENSIFICATION:
  FROM_ITER: 500
  UNTIL_ITER: 12000
  INTERVAL: 100
  MAX_GAUSSIANS: 2500000
  MIN_GAUSSIAN_AGE: 300
INITIALIZATION:
  MODE: colmap
